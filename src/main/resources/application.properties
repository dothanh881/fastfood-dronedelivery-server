# Database Configuration
# Ưu tiên biến môi trường bạn cung cấp (DB_*), fallback sang biến mặc định Railway (MYSQL*), cuối cùng là cấu hình local
spring.datasource.url=jdbc:mysql://${DB_HOST:${MYSQLHOST:localhost}}:${DB_PORT:${MYSQLPORT:3306}}/${DB_NAME:${MYSQLDATABASE:fastfood_db}}?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USERNAME:${MYSQLUSER:root}}
spring.datasource.password=${DB_PASSWORD:${MYSQLPASSWORD:123456}}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Configuration
app.jwt.secret=ThisIsASecretKeyForJwtHs512ThisIsASecretKeyForJwtHs512
app.jwt.expiration=86400000
app.jwt.refresh-expiration=604800000

# WebSocket Configuration
spring.websocket.path=/ws

# VNPay Configuration
vnPay.version=2.1.0
vnPay.command=pay
vnPay.orderType=other
vnPay.currCode=VND
vnPay.hash-secret=ANAIDKYHLTU5HVKRMUM4NVRMLCN0SJU1
vnPay.tmn-code=8VWZT7KJ
vnPay.payment-url=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
vnPay.return-url=${VNPAY_RETURN_URL:https://fastfood-dronedelivery-cnpm.vercel.app/checkout/payment-callback}

# Server Configuration
# Railway cung cấp biến PORT động, dùng nó nếu có để dịch vụ lắng nghe đúng cổng
server.port=${PORT:8080}
server.servlet.context-path=/api

# App Timezone (giúp đồng bộ format thời gian)
spring.jackson.time-zone=Asia/Ho_Chi_Minh

# Swagger UI config (springdoc)
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui
springdoc.swagger-ui.operationsSorter=alpha
springdoc.swagger-ui.tagsSorter=alpha

# Logging Configuration
logging.level.org.springframework.web=INFO
logging.level.org.hibernate=ERROR
logging.level.com.fastfood=DEBUG
logging.level.org.springframework.security=DEBUG

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=20MB

# Upload directory configuration (relative to project root)
app.upload-dir=uploads
app.images.dir=uploads/products
app.public-base=

# Cloudinary Configuration
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dfrkjvmym}
cloudinary.api-key=${CLOUDINARY_API_KEY:786228823246937}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:NhekeYfnbhHjQiJvlEHJr4KC3f8}
cloudinary.secure=true


